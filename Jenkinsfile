pipeline {
    agent {
        label 'master-label'
    }

    stages {
        stage('Checkout') {
            steps {
                echo 'Pulling latest from stash'
            }
        }
    }
}
